<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sent from crm.lead._bsi_notify_slot_conflict (see models/crm_lead.py) when a lead
         reaches a Won stage but its chosen chair/artist/slot has since been taken by another
         booking — instead of blocking the whole Won transaction with a raised error, the
         customer gets this email asking them to pick a different time. -->
    <record id="bsi_salon_slot_conflict_mail_template" model="mail.template">
        <field name="name">Salon: Requested Slot No Longer Available</field>
        <field name="model_id" ref="crm.model_crm_lead"/>
        <field name="subject">Your requested time at {{ object.bsi_location_id.name or 'our salon' }} is no longer available</field>
        <field name="email_to">{{ object.email_from }}</field>
        <field name="auto_delete" eval="True"/>
        <field name="body_html" type="html">
<table border="0" cellpadding="0" cellspacing="0" style="padding-top: 16px; background-color: #F1F1F1; font-family: Arial, sans-serif; color: #333; width: 100%; border-collapse: separate;">
<tr><td align="center">
<table border="0" cellpadding="0" cellspacing="0" width="590" style="padding: 24px; background-color: white; border-collapse: separate; border-radius: 8px;">
<tbody>
    <tr><td style="font-size: 14px; line-height: 1.6;">
        <p>Hi <t t-out="object.contact_name or (object.partner_id and object.partner_id.name) or 'there'">there</t>,</p>
        <p>
            We're sorry — the time you requested at
            <t t-out="object.bsi_location_id.name or 'our salon'">our salon</t>
            on <t t-out="object.bsi_preferred_date or ''"/>
            <t t-if="object.bsi_appointment_time">at <t t-out="object.bsi_appointment_time"/></t>
            <t t-if="object.bsi_artist_id"> with <t t-out="object.bsi_artist_id.name"/></t>
            is no longer available — someone else booked it just before we could confirm yours.
        </p>
        <p t-if="ctx.get('bsi_suggested_slot_name')">
            Good news — the "<t t-out="ctx.get('bsi_suggested_slot_name')"/>" slot on the same day is open
            <t t-if="object.bsi_artist_id"> with <t t-out="object.bsi_artist_id.name"/></t>,
            if that works for you.
        </p>
        <p>Could you get back to us with another time that works, or visit our website to pick a new slot? We'll get you booked in right away once we hear back.</p>
        <p>Sorry for the inconvenience, and thank you for your patience!</p>
        <p style="margin-top: 24px;">
            <t t-out="object.bsi_location_id.name or 'The Team'"/>
        </p>
    </td></tr>
</tbody>
</table>
</td></tr>
</table>
        </field>
    </record>
</odoo>
